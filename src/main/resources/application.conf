app {
  http {
    host = "localhost"
    port = 8080
  }

  database {
    url = "jdbc:postgresql://localhost:5532/cats"
    user = "cats"
    password = "cats"
    driver = "org.postgresql.Driver"
    connection-pool-size = 10
  }

  jwt {
    secret-key = "your-secret-key-here-make-it-long-and-secure-in-production"
    access-token-expiration = "15 minutes"
    refresh-token-expiration = "30 days"
  }

  bcrypt {
    cost = 12
  }
}